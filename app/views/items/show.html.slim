p#notice = notice
.row
  .col-md-12.pull-right
    = link_to "交渉開始", item_offers_path(@item), method: :post unless @item.offered?(current_user)

.row
  .col-md-6
    = image_tag @item.image1.url
    = image_tag @item.image2.url
    = image_tag @item.image3.url
  .col-md-6
    p.text-muted
      strong Name:
    p
      = @item.name

      strong Description:
    p
      = @item.description
    p
      strong Category:
    p
      = @item.category
    p
      strong Prefecture:
    p
       = @item.prefecture
    p
      strong City:
    p
      = @item.city
    p
      strong Classification:
    p
      = @item.classification
    p
      strong Status:
    p
      = @item.status
    p
      strong User:
      = @item.user_id

    = link_to 'Edit', edit_item_path(@item)
    '|
    = link_to 'Back', items_path


- if @item.offered?(current_user)
  p ここにコメント欄